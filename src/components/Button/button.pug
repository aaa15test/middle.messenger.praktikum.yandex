button.button(type=type name=name class=className style=style)
  if childrenType === 'bars' 
    p.barsIcon ☰
  else if childrenType === 'send'
    .sendIcon ➔
  else
    p.buttonLabel(style=labelStyle class=className)= label
