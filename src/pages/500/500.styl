@import('../../static/')

.bugRoot
  display flex
  align-items center
  margin-top 36px

.title
  font-size 40px
  font-weight bold
  text-align center

.message
  margin-top 32px
  text-align center
  font-size 24px

.backChats
  margin-top 163px
  display flex
  justify-content center
